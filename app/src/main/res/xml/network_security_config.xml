<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP globally for workshop CDN compatibility. Authenticated Steam HTTP is gated in app code. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
